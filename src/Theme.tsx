import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#144883',
        },
        secondary: {
            main: '#e9727c',
        },
        background: {
            default: '#3c3c3c',
        }
    },
});